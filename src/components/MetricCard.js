export function MetricCard({ label, value, detail = "" } = {}) { return `<article class="flex min-h-[92px] flex-col justify-center rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><span class="text-[11px] text-slate-500">${label}</span><strong class="my-0.5 text-2xl text-brand-950">${value}</strong>${detail ? `<small class="text-[11px] text-slate-500">${detail}</small>` : ""}</article>`; }
