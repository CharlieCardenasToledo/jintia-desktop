export function EmptyState({ title, description, action = "" } = {}) { return `<div class="flex min-h-[240px] flex-col items-center justify-center p-8 text-center"><div class="mb-[18px] text-lg tracking-[2px] text-brand-600/60" aria-hidden="true">●━━━━○━━━━○</div><h2 class="text-base font-semibold text-brand-950">${title}</h2><p class="my-1.5 mb-[18px] max-w-[420px] leading-relaxed text-slate-500">${description}</p>${action}</div>`; }
